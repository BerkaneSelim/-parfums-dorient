{% extends 'base.html.twig' %}

{% block title %}Détails du message{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-10">
        <div class="card shadow-sm">
            <div class="card-body p-4">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h1>Message SAV</h1>
                    {% if message.status == 'nouveau' %}
                        <span class="badge bg-warning fs-5">🆕 Nouveau</span>
                    {% elseif message.status == 'en_cours' %}
                        <span class="badge bg-info fs-5">⏳ En cours</span>
                    {% elseif message.status == 'traite' %}
                        <span class="badge bg-success fs-5">✅ Traité</span>
                    {% else %}
                        <span class="badge bg-secondary fs-5">{{ message.status }}</span>
                    {% endif %}
                </div>

                <div class="row mb-4">
                    <div class="col-md-6">
                        <div class="card bg-light">
                            <div class="card-body">
                                <h5>👤 Informations client</h5>
                                <p class="mb-1"><strong>Nom :</strong> {{ message.user.nom }} {{ message.user.prenom }}</p>
                                <p class="mb-0"><strong>Email :</strong> {{ message.user.email }}</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="card bg-light">
                            <div class="card-body">
                                <h5>📋 Détails du message</h5>
                                <p class="mb-1"><strong>Date :</strong> {{ message.createdAt ? message.createdAt|date('d/m/Y à H:i') : '' }}</p>
                                <p class="mb-0"><strong>Sujet :</strong> {{ message.subject }}</p>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="card mb-4" style="border-left: 4px solid #8b6f47;">
                    <div class="card-body">
                        <h5>💬 Message du client</h5>
                        <p class="mb-0">{{ message.message|nl2br }}</p>
                    </div>
                </div>

                {% if message.response %}
                <div class="card mb-4" style="border-left: 4px solid #8a9a5b;">
                    <div class="card-body">
                        <h5>✉️ Réponse de l'administrateur</h5>
                        <p class="mb-0">{{ message.response|nl2br }}</p>
                    </div>
                </div>
                {% else %}
                <div class="alert alert-warning">
                    ⚠️ Aucune réponse n'a encore été donnée à ce message
                </div>
                {% endif %}

                <div class="d-flex justify-content-between align-items-center mt-4">
                    <div>
                        <a href="{{ path('app_message_index') }}" class="btn btn-secondary me-2">
                            ← Retour à la liste
                        </a>
                        <a href="{{ path('app_message_edit', {'id': message.id}) }}" class="btn btn-warning">
                            ✏️ Répondre
                        </a>
                    </div>
                    
                    {{ include('message/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
