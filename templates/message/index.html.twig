{% extends 'base.html.twig' %}

{% block title %}Messages SAV{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Gestion des messages SAV</h1>
    <a href="{{ path('app_message_new') }}" class="btn" style="background-color: #8b6f47; color: white;">
        ➕ Nouveau message
    </a>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <table class="table table-hover">
            <thead style="background-color: #f5e6d3;">
                <tr>
                    <th>Date</th>
                    <th>Client</th>
                    <th>Sujet</th>
                    <th>Statut</th>
                    <th>Réponse</th>
                    <th class="text-end">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for message in messages %}
                <tr>
                    <td>{{ message.createdAt ? message.createdAt|date('d/m/Y H:i') : '' }}</td>
                    <td>{{ message.user.nom }} {{ message.user.prenom }}</td>
                    <td><strong>{{ message.subject }}</strong></td>
                    <td>
                        {% if message.status == 'nouveau' %}
                            <span class="badge bg-warning">🆕 Nouveau</span>
                        {% elseif message.status == 'en_cours' %}
                            <span class="badge bg-info">⏳ En cours</span>
                        {% elseif message.status == 'traite' %}
                            <span class="badge bg-success">✅ Traité</span>
                        {% else %}
                            <span class="badge bg-secondary">{{ message.status }}</span>
                        {% endif %}
                    </td>
                    <td>
                        {% if message.response %}
                            <span class="text-success">✅ Répondu</span>
                        {% else %}
                            <span class="text-muted">❌ Pas de réponse</span>
                        {% endif %}
                    </td>
                    <td class="text-end">
                        <a href="{{ path('app_message_show', {'id': message.id}) }}" class="btn btn-sm btn-info">
                            👁️ Voir
                        </a>
                        <a href="{{ path('app_message_edit', {'id': message.id}) }}" class="btn btn-sm btn-warning">
                            ✏️ Répondre
                        </a>
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="6" class="text-center text-muted">Aucun message trouvé</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
