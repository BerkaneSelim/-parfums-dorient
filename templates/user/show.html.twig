{% extends 'base.html.twig' %}

{% block title %}Détails du membre{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow-sm">
            <div class="card-body p-4">
                <h1 class="mb-4">Détails du membre</h1>

                <table class="table table-bordered">
                    <tbody>
                        <tr>
                            <th style="background-color: #f5e6d3; width: 30%;">ID</th>
                            <td>{{ user.id }}</td>
                        </tr>
                        <tr>
                            <th style="background-color: #f5e6d3;">Nom</th>
                            <td><strong>{{ user.nom }}</strong></td>
                        </tr>
                        <tr>
                            <th style="background-color: #f5e6d3;">Prénom</th>
                            <td>{{ user.prenom }}</td>
                        </tr>
                        <tr>
                            <th style="background-color: #f5e6d3;">Email</th>
                            <td>{{ user.email }}</td>
                        </tr>
                        <tr>
                            <th style="background-color: #f5e6d3;">Rôle</th>
                            <td>
                                {% if 'ROLE_ADMIN' in user.roles %}
                                    <span class="badge bg-danger">👑 Administrateur</span>
                                {% else %}
                                    <span class="badge bg-secondary">👤 Utilisateur</span>
                                {% endif %}
                            </td>
                        </tr>
                    </tbody>
                </table>

                <div class="d-flex justify-content-between align-items-center mt-4">
                    <div>
                        <a href="{{ path('app_user_index') }}" class="btn btn-secondary me-2">
                            ← Retour à la liste
                        </a>
                        <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="btn btn-warning">
                            ✏️ Modifier
                        </a>
                    </div>
                    
                    {{ include('user/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
