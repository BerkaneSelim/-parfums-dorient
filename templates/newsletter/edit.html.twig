{% extends 'base.html.twig' %}

{% block title %}Modifier l'abonné{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow-sm">
            <div class="card-body p-4">
                <h1 class="mb-4">Modifier l'abonné</h1>

                {{ include('newsletter/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

                <div class="mt-4 d-flex justify-content-between align-items-center">
                    <a href="{{ path('app_newsletter_index') }}" class="btn btn-secondary">
                        ← Retour à la liste
                    </a>
                    
                    {{ include('newsletter/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}