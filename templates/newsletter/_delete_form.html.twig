<form method="post" action="{{ path('app_newsletter_delete', {'id': newsletter.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet abonné ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ newsletter.id) }}">
    <button class="btn btn-danger">
        🗑️ Supprimer
    </button>
</form>
